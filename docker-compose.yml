version: "3.7"
services:
  exploit-server:
    build: exploit-server
    network_mode: host
    volumes:
      - type: bind
        source: "./exploit-server"
        target: "/exploit_dir"
  cve-44228:
    build: vulnerable-app
    network_mode: host
  cve-45046:
    build: vulnerable-app
    network_mode: host 

